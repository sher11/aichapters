theme: minima
title: AIChapters
description: Learn AI and Machine Learning through structured courses

# Collections
collections:
  courses:
    output: true
    permalink: /courses/:path/

# Defaults
defaults:
  - scope:
      path: "courses"
    values:
      layout: "chapter"
